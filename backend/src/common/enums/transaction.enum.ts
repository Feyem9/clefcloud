export enum TransactionStatus {
  PENDING = 'pending',
  SUCCESS = 'success',
  FAILED = 'failed',
  EXPIRED = 'expired',
}

export enum TransactionType {
  PARTITION = 'partition',
  PREMIUM = 'premium',
}
